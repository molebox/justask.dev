---
title: Pass options to gatsby-config.js
date: 2019-12-05
published: true
category: gatsby
---
import Text from '../../src/components/Text'

<Text>To pass options to the gatsby-config.js </Text>

```js
module.exports = (options) => {
    const {
        lots,
        and,
        lots,
        and,
        lots,
        of,
        options
    } = options;

   return {
    plugins: [...myplugins]
  };
```